# プロジェクト: 1歳半向けドライブアプリ

- 仕様の正本は `DESIGN.md`。実装前に必ず全文を読む
- 実装は `DESIGN.md` 17章の段階単位。指示された段階だけを作り、終わったら合格条件を1つずつ確認して報告する
- 調整値は `js/config.js` の `CFG` に集める。値を変えたら理由を報告する
- `DESIGN.md` 18章「やってはいけないこと」を守る
- 画像素材の作成は `/gpt-asset-loop` スキルで行う。アプリ側は素材が無くてもプレースホルダーで動くように作る
- このセッションはクラウドで動き、ユーザーはiPhoneから操作している。変更は作業ブランチにコミットし、段階ごとにプルリクエストを作る
- 動作確認はNetlifyのデプロイプレビューをiPhoneのSafariで開いて行う。報告には、そのURLで何を確かめればよいかを書く
